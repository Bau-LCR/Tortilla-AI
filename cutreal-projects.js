/* CUT-REAL PROJECTS · chats y archivos de proyecto separados */
(function () { 'use strict'; const $ = id => document.getElementById(id); const key = 'cutreal-projects-state'; const state = { projects: [], active: null }; const defaults = () => ({ id: `project-${Date.now()}`, name: 'Nuevo proyecto', category: 'programacion', messages: [], files: { 'index.html': '<!doctype html>\n<html><body><h1>Proyecto</h1></body></html>', 'style.css': 'body { font-family: sans-serif; }', 'script.js': 'console.log("Proyecto listo");' }, activeFile: 'index.html' }); function load() { try { const data = JSON.parse(localStorage.getItem(key) || '{}'); state.projects = Array.isArray(data.projects) ? data.projects : []; state.active = data.active || state.projects[0]?.id || null; } catch (_) {} } function save() { localStorage.setItem(key, JSON.stringify(state)); } function active() { return state.projects.find(item => item.id === state.active); } function open() { const panel = $('cutreal-projects'); if (!panel) return false; panel.hidden = false; panel.style.display = 'flex'; render(); return true; } function close() { const panel = $('cutreal-projects'); if (panel) { panel.hidden = true; panel.style.display = 'none'; } return true; } function create() { const project = defaults(); project.name = $('cutreal-project-name')?.value.trim() || project.name; project.category = $('cutreal-project-category')?.value || project.category; state.projects.unshift(project); state.active = project.id; save(); render(); } function renderList() { const list = $('cutreal-project-list'); if (!list) return; list.innerHTML = state.projects.map(item => `<button type="button" class="cr-project-item ${item.id === state.active ? 'active' : ''}" data-project-id="${item.id}"><b>${item.name}</b><small>${item.category}</small></button>`).join('') || '<p class="cr-project-empty">Creá tu primer proyecto.</p>'; list.querySelectorAll('[data-project-id]').forEach(button => button.addEventListener('click', () => { state.active = button.dataset.projectId; save(); render(); })); } function renderEditor() { const project = active(); const editor = $('cr-project-editor'); const tabs = $('cr-project-file-tabs'); if (!project || !editor || !tabs) return; tabs.innerHTML = Object.keys(project.files).map(file => `<button type="button" class="${file === project.activeFile ? 'active' : ''}" data-project-file="${file}">${file}</button>`).join(''); tabs.querySelectorAll('[data-project-file]').forEach(button => button.addEventListener('click', () => { project.activeFile = button.dataset.projectFile; save(); renderEditor(); })); editor.value = project.files[project.activeFile] || ''; editor.oninput = () => { project.files[project.activeFile] = editor.value; save(); }; } function run() { const project = active(); const frame = $('cr-project-preview'); if (!project || !frame) return; frame.srcdoc = `${project.files['index.html'] || ''}<style>${project.files['style.css'] || ''}</style><script>${project.files['script.js'] || ''}<\/script>`; $('cr-project-status').textContent = 'Preview actualizado'; } async function send() { const project = active(); const input = $('cr-project-input'); const chat = $('cr-project-chat'); const text = input?.value.trim(); if (!project || !text || !chat) return; input.value = ''; project.messages.push({ role: 'user', content: text }); chat.insertAdjacentHTML('beforeend', `<div class="cr-project-msg user"><b>Vos:</b> ${text.replace(/[<>&]/g, c => ({ '<':'&lt;','>':'&gt;','&':'&amp;' }[c]))}</div>`); try { const response = await fetch('/api/chat', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ mensajes: [{ role: 'system', content: `Sos el asistente exclusivo del proyecto ${project.name}. Categoría: ${project.category}. No mezcles contexto de Sandbox, SUPER ni Chat Normal. Archivos actuales: ${JSON.stringify(project.files)}` }, ...project.messages.slice(-12)] }) }); const data = await response.json().catch(() => ({})); const reply = data.choices?.[0]?.message?.content || data.error || 'La IA del proyecto no respondió.'; project.messages.push({ role: 'assistant', content: reply }); chat.insertAdjacentHTML('beforeend', `<div class="cr-project-msg ai"><b>IA del proyecto:</b> ${String(reply).replace(/[<>&]/g, c => ({ '<':'&lt;','>':'&gt;','&':'&amp;' }[c]))}</div>`); save(); } catch (error) { chat.insertAdjacentHTML('beforeend', `<div class="cr-project-msg ai">Error: ${error.message}</div>`); } } function render() { renderList(); renderEditor(); const project = active(); const chat = $('cr-project-chat'); if (chat && project) chat.innerHTML = project.messages.map(item => `<div class="cr-project-msg ${item.role === 'user' ? 'user' : 'ai'}"><b>${item.role === 'user' ? 'Vos' : 'IA del proyecto'}:</b> ${String(item.content).replace(/[<>&]/g, c => ({ '<':'&lt;','>':'&gt;','&':'&amp;' }[c]))}</div>`).join(''); } function bind() { $('cr-project-close')?.addEventListener('click', close); $('cutreal-project-create')?.addEventListener('click', create); $('cr-project-run')?.addEventListener('click', run); $('cr-project-send')?.addEventListener('click', send); $('cr-project-input')?.addEventListener('keydown', e => { if (e.key === 'Enter' && !e.shiftKey) { e.preventDefault(); send(); } }); } load(); document.addEventListener('click', event => { if (event.target.closest('#cutreal-project-create')) create(); if (event.target.closest('#cr-project-close')) close(); if (event.target.closest('#cr-project-run')) run(); if (event.target.closest('#cr-project-send')) send(); }); window.CutRealProjects = { open, close, create, getState: () => state }; if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', bind, { once: true }); else bind(); })();
