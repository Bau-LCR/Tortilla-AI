/* PROJECT CHAT API · proveedor separado del Chat Normal */
function json(res,status,payload){res.status(status).setHeader('Content-Type','application/json');return res.end(JSON.stringify(payload))}
export default async function handler(req,res){if(req.method!=='POST')return json(res,405,{error:'Método no permitido'});const base=String(process.env.PROJECTS_API_BASE_URL||'').replace(/\/$/,'');const key=process.env.PROJECTS_API_KEY;if(!base||!key)return json(res,503,{error:'Faltan PROJECTS_API_BASE_URL y PROJECTS_API_KEY en Vercel.'});const messages=Array.isArray(req.body?.mensajes)?req.body.mensajes:[];if(!messages.length)return json(res,400,{error:'Faltan mensajes del proyecto.'});const configured=String(process.env.PROJECTS_MODEL||'openai/gpt-oss-20b').trim();const requested=String(req.body?.providerModel||'').trim();const raw=requested&& !['basic','pro','ultra'].includes(requested)?requested:configured;const first=raw==='llama-3.3-70b-versatile'?'openai/gpt-oss-20b':raw;const candidates=[...new Set([first,'openai/gpt-oss-20b','openai/gpt-oss-120b','llama-3.1-8b-instant'])];const timeoutMs=Number(process.env.PROJECTS_TIMEOUT_MS||90000);for(const model of candidates){const controller=new AbortController();const timer=setTimeout(()=>controller.abort(),timeoutMs);try{const upstream=await fetch(`${base}/chat/completions`,{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${key}`},body:JSON.stringify({model,messages,temperature:.25,max_tokens:Number(process.env.PROJECTS_MAX_OUTPUT_TOKENS||1800)}),signal:controller.signal});const data=await upstream.json().catch(()=>({}));if(upstream.ok)return json(res,200,{...data,projectProvider:true,model});const detail=String(data.error?.message||'');const modelError=/model|does not exist|access|permission|not found/i.test(detail);if(!modelError)return json(res,upstream.status,{error:detail||`Proveedor de Proyectos HTTP ${upstream.status}`})}catch(error){if(error.name==='AbortError')return json(res,504,{error:'Tiempo agotado en el proveedor de Proyectos.'});if(model===candidates.at(-1))return json(res,502,{error:'No se pudo contactar al proveedor de Proyectos.'})}finally{clearTimeout(timer)}}return json(res,403,{error:'La clave de Proyectos no tiene acceso a los modelos activos configurados. Probá openai/gpt-oss-20b en PROJECTS_MODEL.'})}
